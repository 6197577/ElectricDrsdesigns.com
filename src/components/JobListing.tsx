import React from 'react';import styles from '../styles/JobListing.module.css';interface Props {  title: string;  location: string;  type: 'Full-Time' | 'Part-Time' | 'Contract';  description: string;}const JobListing = ({ title, location, type, description }: Props) => {  return (    <div className={styles.jobCard}>      <div className={styles.header}>        <h3>{title}</h3>        <span className={styles.typeBadge}>{type}</span>      </div>      <p style={{fontStyle:'italic', color:'#ccc', marginBottom:'10px'}}>{location}</p>      <p>{description}</p>      <a href="mailto:careers@electricdrsdesigns.com" className="btn" style={{marginTop:'20px'}}>Apply Now</a>    </div>  );};export default JobListing;